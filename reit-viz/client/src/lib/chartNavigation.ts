// Stub — TODO: reverse-engineer from production bundle
// Provides imperative helpers for navigating to the Charts page (with optional
// ticker / pair pre-selection).

/** Navigate to the Charts page for a single ticker. */
export function navigateToCharts(ticker: string, _options?: Record<string, any>): void {
  // Stub — TODO: reverse-engineer from production bundle
}

/** Navigate to the Charts page with a pair of tickers pre-selected. */
export function navigateToChartsWithPair(
  legA: string,
  legB: string,
  _options?: Record<string, any>
): void {
  // Stub — TODO: reverse-engineer from production bundle
}

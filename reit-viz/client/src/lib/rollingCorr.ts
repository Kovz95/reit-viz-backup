// Stub — TODO: reverse-engineer algorithm from production bundle

/**
 * Compute a rolling Pearson correlation between two numeric series.
 * Returns an array of the same length with null for positions where the window
 * is not yet full.
 */
export function computeRollingCorr(
  _seriesA: number[],
  _seriesB: number[],
  _window: number,
  _lag?: number
): (number | null)[] {
  return [];
}

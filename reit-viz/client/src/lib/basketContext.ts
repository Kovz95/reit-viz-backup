// Stub — re-exports useBaskets under the basketContext module name.
// basketContext and basketsContext are thin wrappers around useBaskets.
export { useBaskets } from "@/lib/useBaskets";
export type { Basket, BasketOptions, UseBasketsReturn } from "@/lib/useBaskets";

// Stub — TODO: reverse-engineer from production bundle

/**
 * Fetch the list of historical earnings announcement dates for a ticker.
 * Returns ISO date strings sorted ascending.
 */
export async function fetchEarningsDates(
  _ticker: string,
  _options?: Record<string, any>
): Promise<string[]> {
  // Stub — TODO: reverse-engineer from production bundle
  return [];
}

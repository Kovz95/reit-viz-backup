# Copy to .env (which is gitignored) and fill in real values. Never commit .env.
# On the Vultr server this lives at /opt/reit-viz/.env and is loaded by
# ecosystem.config.cjs (process.loadEnvFile) before pm2 starts the app.

# Anthropic API key — rotate in the Anthropic Console; do NOT hardcode anywhere.
ANTHROPIC_API_KEY=sk-ant-api03-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
